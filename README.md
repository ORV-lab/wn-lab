# WN-Lab

Monorepo scaffold for a desktop-first web project with:

- `frontend`: Next.js + TypeScript app
- `backend`: FastAPI service

The initial frontend includes a first-pass home/library screen based on the provided PDF reference.
